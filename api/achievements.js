const {db,json}=require('./_lib');
module.exports=async(req,res)=>{try{const s=db();const {data:o,error}=await s.from('orders').select('order_date,paid');if(error)throw error;const by={};for(const x of o||[])by[x.order_date]=(by[x.order_date]||0)+(x.paid||0);const max=Math.max(0,...Object.values(by)),all=Object.values(by).reduce((a,x)=>a+x,0);const {count}=await s.from('clients').select('*',{count:'exact',head:true}).eq('active',true);json(res,200,{items:[{icon:'🥉',title:'첫 100개',done:max>=100},{icon:'🥈',title:'하루 200개',done:max>=200},{icon:'🥇',title:'하루 300개',done:max>=300},{icon:'⚓',title:'거래처 10척',done:(count||0)>=10},{icon:'🍱',title:'누적 10,000개',done:all>=10000},{icon:'👑',title:'성수기 평균 250개',done:false}]})}catch(e){json(res,500,{error:e.message})}};