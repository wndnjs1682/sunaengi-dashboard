const {db,json}=require('./_lib');
module.exports=async(req,res)=>{const s=db();try{
 if(req.method==='GET'){const {data,error}=await s.from('clients').select('*').order('sort_order');if(error)throw error;return json(res,200,{clients:data})}
 if(req.method==='POST'){const b=req.body||{};const row={id:b.id||('c'+Date.now()),sort_order:Number(b.sort_order)||999,name:b.name,affiliation:b.affiliation||'',grade:b.grade||'C',source:b.source||'수기',capacity:Number(b.capacity)||22,sunsang_group:b.sunsang_group||null,ship_name:b.ship_name||b.name,thefishing_uid:b.thefishing_uid||null,captain_on:b.captain_on!==false,captain_meal:b.captain_meal!==false,manager_on:!!b.manager_on,manager_meal:!!b.manager_meal,soup_separate:!!b.soup_separate,delivery:b.delivery||'',reservation_url:b.reservation_url||'',active:b.active!==false,unit_price:Number(b.unit_price)||8000,vat_mode:b.vat_mode||'VAT별도',tax_enabled:b.tax_enabled!==false,tax_cycle:b.tax_cycle||'월합산',tax_group:b.tax_group||b.name,memo:b.memo||''};const {data,error}=await s.from('clients').upsert(row).select().single();if(error)throw error;return json(res,200,{ok:true,client:data})}
 if(req.method==='DELETE'){const id=req.query.id;if(!id)return json(res,400,{error:'id 필요'});const {error}=await s.from('clients').update({active:false}).eq('id',id);if(error)throw error;return json(res,200,{ok:true})}
 if(req.method==='PATCH'){const b=req.body||{};if(!Array.isArray(b.order))return json(res,400,{error:'order 필요'});for(let i=0;i<b.order.length;i++){const {error}=await s.from('clients').update({sort_order:i+1}).eq('id',b.order[i]);if(error)throw error}return json(res,200,{ok:true})}
 return json(res,405,{error:'method'});
}catch(e){json(res,500,{error:e.message})}};